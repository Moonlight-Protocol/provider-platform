// Callback endpoints removed — PP now determines its own state via
// on-chain queries and the council's public membership-status endpoint.
// See refresh-status in dashboard/council.ts for the replacement.
